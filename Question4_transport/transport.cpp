#include "cargaison.hpp"

#include <algorithm>
#include <assert.h>
#include <vector>

void transport(unsigned int nb_villes,
	       const std::vector<Cargaison>& cargaisons,
	       std::vector<Cargaison>& sortie) {

  // INSEREZ VOTRE CODE ICI

  // Vous pouvez supposer que pour une paire de ville (i, j), il n'y a
  // qu'une seule cargaison et qu'aucune cargaison ne relie une ville
  // à elle-même. La taille du vecteur cargaisons est donc au plus
  // nb_villes * (nb_villes - 1) / 2.
}
