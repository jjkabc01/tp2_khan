#include "tri.hpp"

bool comparaison(const std::pair<int, int>& a, const std::pair<int, int>& b) {
  return a.first < b.first;
}

// Vous pouvez declarer de nouvelles fonctions ici.

int main(void) {
  // Inserez ici le code de vos experimentations. Ce code n'a pas
  // besoin de compiler sur Linux. Vous devrez entre autres utiliser
  // des fonctions, propre a votre systeme d'exploitation, pour
  // calculer le temps d'execution des diverses fonctions.  Ce code
  // doit imprimer des donnees qui pourront etre importees dans un
  // logiciel (ex: un tableur) pouvant produire votre graphique.

  return 0;
}
